// This file allows us to apply the plugin in this build too

apply from: '../tidal-app-plugin/build.gradle'

sourceSets {
    main {
        java {
            srcDir '../tidal-app-plugin/src/main/java'
        }
    }
}
